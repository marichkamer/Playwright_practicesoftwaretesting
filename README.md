Create an initial setup of Playwright on the local machine
Create Playwright config if it does not exist and familiarize
Create first specs using the existing BDD scenario created in Module 2 (automate only 4 of BDD scenarios)
Execute these tests using CLI in different browsers (Chrome, Firefox, Safari) in headless mode
Execute tests in parallel using 2 instances
Add the option to run tests 2 times before marking it as failed
